/* Reset */
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: Arial, Helvetica, sans-serif; line-height: 1.6; background: #f5f5f5; color: #333; }

/* Container */
.container { width: 1200px; max-width: 95%; margin: 0 auto; }

/* Header */
.site-header { background: #006699; color: white; padding: 20px 0; }
.site-header h1 { font-size: 26px; text-align: center; }
.site-header p { text-align: center; font-size: 14px; }

/* Menu */
.navbar { background: #004466; }
.navbar ul { display: flex; list-style: none; justify-content: center; }
.navbar ul li { }
.navbar ul li a {
  display: block; padding: 12px 20px; color: white; text-decoration: none;
}
.navbar ul li a:hover { background: #006699; }

/* Main layout */
.main { display: flex; margin: 20px 0; gap: 20px; }
.content { flex: 3; }
.sidebar { flex: 1; }

/* Card box */
.card {
  background: white;
  padding: 16px;
  border: 1px solid #ddd;
  border-radius: 6px;
  margin-bottom: 20px;
}
.card h2 { font-size: 20px; margin-bottom: 12px; color: #006699; }

/* Footer */
.site-footer {
  background: #004466;
  color: white;
  padding: 20px;
  text-align: center;
  font-size: 14px;
  margin-top: 40px;
}
